export default class DropdownSearch {
    constructor(model, field, value, limit) {
        this.model = model;
        this.field = field;
        this.value = value;
        this.limit = limit;
    }
}